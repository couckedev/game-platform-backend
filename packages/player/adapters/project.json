{
  "name": "player-adapters",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/player/adapters/src",
  "projectType": "library",
  "tags": ["type:adapters", "scope:player"],
  "implicitDependencies": ["player-domain", "player-application", "shared-time"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/player/adapters"],
      "options": {
        "command": "npx tsc -p packages/player/adapters/tsconfig.lib.json"
      }
    }
  }
}
