{
  "name": "player-contracts",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/player/contracts/src",
  "projectType": "library",
  "tags": ["type:contracts", "scope:player"],
  "implicitDependencies": ["player-domain", "player-application"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npx tsc -p packages/player/contracts/tsconfig.lib.json"
      }
    }
  }
}
