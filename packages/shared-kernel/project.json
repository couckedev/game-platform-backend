{
  "name": "shared-kernel",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm --dir packages/shared-kernel build"
      }
    }
  },
  "tags": ["type:shared-kernel", "scope:shared"]
}
