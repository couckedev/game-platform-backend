{
  "name": "shared-time",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/time/src",
  "projectType": "library",
  "tags": ["type:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/shared/time"],
      "options": {
        "command": "npx tsc -p packages/shared/time/tsconfig.lib.json"
      }
    }
  }
}
