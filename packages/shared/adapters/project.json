{
  "name": "shared-adapters",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/adapters/src",
  "projectType": "library",
  "tags": ["type:shared"],
  "implicitDependencies": ["shared-time"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npx tsc -p packages/shared/adapters/tsconfig.lib.json"
      }
    }
  }
}
