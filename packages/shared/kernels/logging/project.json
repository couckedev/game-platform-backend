{
  "name": "shared-kernels-logging",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/kernels/logging/src",
  "projectType": "library",
  "tags": ["type:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npx tsc -p packages/shared/kernels/logging/tsconfig.lib.json"
      }
    }
  }
}
