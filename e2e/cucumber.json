{
  "default": {
    "paths": ["features/**/*.feature"],
    "require": ["support/**/*.ts", "step-definitions/**/*.steps.ts"],
    "format": ["progress-bar", "html:reports/cucumber-report.html"]
  }
}
